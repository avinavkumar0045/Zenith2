const fs = require('fs');
const content = fs.readFileSync('src/modules/reports/components/CelestialReport.tsx', 'utf8');

// I will write a script to replace the file or I can just use replace_file_content.
